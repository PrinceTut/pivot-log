// One place for values that were previously duplicated across components.

export const REPO_URL = 'https://github.com/PrinceTut/pivot-log';

// Adobe Fonts web project ID (the "kit" ID shown in Adobe Fonts > Web Projects,
// e.g. 'abc1234'). Leave empty to use the self-hosted fallbacks:
//   Aktiv Grotesk -> Inter Tight, LoRes 12 -> Redaction 35, Victor Mono -> Victor Mono (exact).
// When set, BaseLayout loads the kit and switches to Aktiv Grotesk's own tracking values.
export const ADOBE_KIT_ID = '';
